"""LOP 2 — AI Translation Engine (BUILD_PLAN.md GD4, AI_ROADMAP section 4).

The LLM only classifies feedback into the closed tag enum of VOCABULARY.md;
all Hz/Q/dB numbers come from the deterministic mapping in vocabulary.py,
bounded by safety clamps. engine.propose() only proposes — the UI applies.
"""
